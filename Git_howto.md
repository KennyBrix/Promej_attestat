# Подсказка по GIT

Создание репозитория:
```sh
git init
```
Для добавления всех изменений в вашем файле:
```sh
git add <имя файла>
```
Создание коммитов:
```sh
git commit -m "Message"
```
git add

git log
git log --oneline
git checkout